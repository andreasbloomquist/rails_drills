<h1>Edit <%= @article.title%></h1>

<%= form_for @article, url: "/articles/#{@article.id}", method: "put" do |f| %>
	<div>
		<%= f.text_field :title %>
	</div>
	<div>
		<%= f.text_area :content %>
	</div>
	<%= f.submit "Update" %>
<% end %>

